package game;

import javax.swing.*;

public class ScreenGame extends JFrame {
    public ScreenGame(){
        setTitle("Snake Game");
        setSize(800,800);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}
